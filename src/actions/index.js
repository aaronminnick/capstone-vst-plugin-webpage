import * as a from './ActionTypes';

export const contentFeatures = () => {
  return {
    type: a.CONTENT_FEATURES
  }
};

export const contentListen = () => {
  return {
    type: a.CONTENT_LISTEN
  }
};

export const contentDownload = () => {
  return {
    type: a.CONTENT_DOWNLOAD
  }
};

export const contentContact = () => {
  return {
    type: a.CONTENT_CONTACT
  }
};